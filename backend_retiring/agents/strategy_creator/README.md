# Strategy Creator Department

Home for converting research ideas and user requests into formal strategy specs.

The current facade points to the existing `StrategyCreatorAgent`. Future work should keep spec creation here while deterministic validation remains in strategy services and contracts.
