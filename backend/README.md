# Backend Migration Scaffolding

This directory is reserved for the new agentic system slices described in `docs/agentic_ai/implementation_plan.md`.

The current runtime remains in `apps/` during the additive migration.

