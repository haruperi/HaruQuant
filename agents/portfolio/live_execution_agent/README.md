# Live Execution Agent

Runs only guarded live execution checks. Live mode is disabled by default and every live order requires RiskGovernor approval, a healthy broker heartbeat, healthy audit logging, and a healthy kill switch.
