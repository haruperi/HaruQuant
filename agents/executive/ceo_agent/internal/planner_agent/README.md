# Internal Planner Agent

This is the CEO-owned internal planning facade. It reuses the existing `agents.executive.planner_agent.service.PlannerAgent` implementation and converts its output into the Executive Department planner contract.

It is not a public department agent and does not answer users, call departments directly, approve risk, or execute workflows.
