# Planner Agent

Executive routing agent for HaruQuant. It classifies requests, identifies required evidence, selects safe tools/agents, and hands final synthesis back to CEOAgent through `services/ceo_gateway.py`.
