# Frontend Migration Scaffolding

The active operator UI remains in `ui/`.

This directory reserves space for migration-era shared frontend packages and future dashboard slices without forcing a move of the current Next.js app.

